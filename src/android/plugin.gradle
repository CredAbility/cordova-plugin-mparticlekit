buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        compile 'com.mparticle:android-iterable-kit:5.14.+'
    }
}

allprojects {
    repositories {
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
